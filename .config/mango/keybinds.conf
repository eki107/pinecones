# Compositor
bind=SUPER,r,reload_config

# Launch
bind=SUPER,Return,spawn,rofi -show drun
bind=SHIFT+SUPER,Return,spawn,alacritty

# Windows
bind=SUPER,c,killclient
bind=SUPER,Tab,focusstack,next
bind=SUPER,h,focusstack,left
bind=SUPER,l,focusstack,prev
bind=SUPER,k,focusstack,prev
bind=SUPER,j,focusstack,next
bind=SUPER+SHIFT,k,exchange_stack_client,prev
bind=SUPER+SHIFT,j,exchange_stack_client,next
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right

# Window state
bind=SUPER,g,toggleglobal
bind=ALT,Tab,toggleoverview
bind=ALT,backslash,togglefloating
bind=SUPER+SHIFT,m,togglemaximizescreen
bind=SUPER,f,togglefullscreen
bind=SHIFT+SUPER,f,togglefakefullscreen
bind=SUPER,i,minimized
bind=SUPER,o,toggleoverlay
bind=SUPER+SHIFT,I,restore_minimized

# Layouts
bind=SUPER,n,switch_layout
bind=SUPER,v,setlayout,vertical_tile
bind=SUPER,m,setlayout,monocle

# Tags
bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SHIFT+SUPER,1,tagsilent,1,0
bind=SHIFT+SUPER,2,tagsilent,2,0
bind=SHIFT+SUPER,3,tagsilent,3,0
bind=SHIFT+SUPER,4,tagsilent,4,0

# Monitors
bind=SUPER,comma,focusmon,left
bind=SUPER,period,focusmon,right
bind=SUPER+SHIFT,comma,tagmon,left
bind=SUPER+SHIFT,period,tagmon,right

# Move & resize (floating)
bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0
bind=CTRL+ALT,Up,resizewin,+0,-50
bind=CTRL+ALT,Down,resizewin,+0,+50
bind=CTRL+ALT,Left,resizewin,-50,+0
bind=CTRL+ALT,Right,resizewin,+50,+0

# Media — volume (wpctl / WirePlumber)
# Note: if your keys don't register, run wev to confirm exact key names
bind=NONE,XF86AudioRaiseVolume,spawn,wpctl set-volume @DEFAULT_SINK@ 5%+
bind=NONE,XF86AudioLowerVolume,spawn,wpctl set-volume @DEFAULT_SINK@ 5%-
bind=NONE,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_SINK@ toggle
bind=SHIFT,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_SOURCE@ toggle

# Mouse
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_middle,togglefloating
mousebind=SUPER,btn_right,moveresize,curresize

# Axis
axisbind=SUPER,UP,viewtoleft_have_client
axisbind=SUPER,DOWN,viewtoright_have_client

# Layer rules
layerrule=animation_type_open:zoom,layer_name:rofi
layerrule=animation_type_close:zoom,layer_name:rofi
